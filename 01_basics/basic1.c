#include <stdio.h>
#include <stdlib.h>
int main(){
    // ctrl +  1 + `
    // terminal clear -> terminal band bhi ho jati hai
    // terminal toggle ( khulega or band hojayega )
    // ctrl + ` + ctrl + `
    // `
    // code run 
    // keyboard shortcuts
    // ctrl + k + s

    // alt + q + enter
    // code format


    printf("Hi Anisha");
    // ctrl + d -> copy line down
    printf("Hi Anisha");
    return 0;
}